{
  "name": "infastructure",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "devDependencies": {
    "@aws-sdk/client-dynamodb": "^3.352.0",
    "date-fns": "^2.30.0"
  },
  "scripts": {
    "start": "docker-compose up -d && sleep 10 && yarn dev",
    "stop": "docker-compose down",
    "dev": "node --no-warnings=ExperimentalWarning populate-items.js"
  }
}
