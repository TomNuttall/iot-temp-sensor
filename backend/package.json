{
  "name": "backend",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "devDependencies": {
    "@aws-sdk/client-dynamodb": "^3.352.0",
    "aws-sdk-client-mock": "^4.0.1",
    "vitest": "^4.0.16"
  },
  "scripts": {
    "dev": "sam local start-api",
    "test": "vitest"
  }
}
